# Terminal UI

**Purpose:** Describe how Buddy behaves in the terminal: prompts, commands, and how output is shown. Aligns with the CLI entry in [`src/cli/cli.ts`](src/cli/cli.ts).

**This file will contain:**

- Default prompt or command style.
- How errors and success messages should look (keep them short).
- Any keyboard or session conventions once the CLI grows.
